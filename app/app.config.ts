// Global Nuxt UI theme.
// - primary  = marine (deep navy ink)     → defined in assets/css/main.css
// - secondary = clay (terracotta accent)  → defined in assets/css/main.css
// - neutral  = stone (warm greys)
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'marine',
      secondary: 'clay',
      neutral: 'stone',
    },
  },
})
